{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:base", ":dependencyDashboard", ":semanticCommits", ":timezone(UTC)"],
  "schedule": ["after 9pm every weekday", "before 5am every weekday", "every weekend"],
  "assignees": ["sergii-bomko"],
  "prHourlyLimit": 4,
  "prConcurrentLimit": 8,
  "packageRules": [
    {
      "description": "Legacy scripts/ excluded — not in pnpm workspace, byte-identical per DoD §I",
      "matchPaths": ["scripts/**"],
      "enabled": false
    },
    {
      "description": "Patch updates auto-merge AFTER branch protection requirements are met (1 review + signed commits). Bot does NOT bypass review.",
      "matchUpdateTypes": ["patch"],
      "automerge": true,
      "platformAutomerge": true
    },
    {
      "description": "Major updates require explicit approval via Dependency Dashboard",
      "matchUpdateTypes": ["major"],
      "dependencyDashboardApproval": true
    },
    {
      "description": "GitHub Actions bumps grouped weekly (Monday before 5am UTC)",
      "matchManagers": ["github-actions"],
      "groupName": "github-actions-bump",
      "schedule": ["before 5am on monday"]
    },
    {
      "description": "npm devDependencies grouped weekly (Monday before 5am UTC)",
      "matchDepTypes": ["devDependencies"],
      "groupName": "npm devDependencies (weekly)",
      "schedule": ["before 5am on monday"]
    },
    {
      "description": "Pin Dockerfile base images by digest — resolves ADR-0013 P0b followup; Renovate keeps digests fresh",
      "matchDatasources": ["docker"],
      "pinDigests": true
    }
  ]
}
