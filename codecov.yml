coverage:
  status:
    project:
      default:
        target: 80%
        threshold: 0%
        if_ci_failed: error
    patch:
      default:
        target: 80%
  precision: 2
  round: down

comment:
  layout: 'reach,diff,flags,files,components'
